// components/RidgeDivider.js
// The signature element: a roof-pitch chevron band. Used sparingly
// to separate major sections, echoing an actual ridge line.
export default function RidgeDivider() {
  return <div className="ridge" role="presentation" aria-hidden="true" />;
}
